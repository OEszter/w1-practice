let arrayOfColors = ["fehér", "fekete", "szürke", "kék", "barna", "zöld", "piros", "sárga", "narancs", "lila", "rózsaszín", "lazacrózsaszín"];

//console.log(arrayOfColors.length);

for (let index = 0; index < arrayOfColors.length; index++) {
    if (arrayOfColors[index].includes('í')) {
        console.log(`A ${arrayOfColors[index]} tartalmaz 'í' betűt.`)
        } else {
            console.log(`A ${arrayOfColors[index]} nem tartalmaz 'í' betűt.`)
        }
}

for (let index = 0; index < arrayOfColors.length; index++) {
    
}